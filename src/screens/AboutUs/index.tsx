import React, {memo} from 'react';
import {SafeAreaView, Text} from 'react-native';

const AboutUs = () => (
  <SafeAreaView>
    <Text>About Us</Text>
  </SafeAreaView>
);

export default memo(AboutUs);
